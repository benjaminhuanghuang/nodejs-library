mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    h2 I forgot my password!
    .form-group
      label(for="email") Email
      input.form-control(type="email" name="email")
    button.btn.btn-primary.btn-lg.center-block(type="submit") Send a Reset